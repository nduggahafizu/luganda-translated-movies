╔══════════════════════════════════════════════════════════════════════╗
║                                                                      ║
║              🚂 RAILWAY BACKEND DEPLOYMENT GUIDE 🚂                  ║
║                                                                      ║
╚══════════════════════════════════════════════════════════════════════╝

Status: ✅ READY FOR RAILWAY
Time Needed: ~20 minutes

═══════════════════════════════════════════════════════════════════════

                    YES! RAILWAY CAN RUN YOUR BACKEND!

═══════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────┐
│ WHY RAILWAY IS PERFECT                                              │
└─────────────────────────────────────────────────────────────────────┘

  ✅ Free Tier: $5 credit/month (enough for small apps)
  ✅ Easy Setup: Deploy in minutes
  ✅ MongoDB Support: Built-in plugin or Atlas
  ✅ Auto-Deploy: Connects to GitHub
  ✅ Node.js 22: Latest version supported
  ✅ Environment Variables: Easy to configure
  ✅ Custom Domains: Free SSL
  ✅ Real-time Logs: Monitor everything

┌─────────────────────────────────────────────────────────────────────┐
│ WHAT'S ALREADY READY                                                │
└─────────────────────────────────────────────────────────────────────┘

  ✅ Backend code tested and working
  ✅ Railway config files created:
     - railway.json
     - nixpacks.toml
     - .railwayignore
  ✅ Package.json with start script
  ✅ Environment variables documented
  ✅ MongoDB connection code ready
  ✅ CORS configured
  ✅ Security features enabled

┌─────────────────────────────────────────────────────────────────────┐
│ QUICK DEPLOYMENT STEPS                                              │
└─────────────────────────────────────────────────────────────────────┘

  STEP 1: Go to Railway (2 min)
  → Visit: https://railway.app
  → Sign in with GitHub
  → Click "New Project"
  → Select "Deploy from GitHub repo"
  → Choose your repository

  STEP 2: Configure Service (3 min)
  → Click on the service
  → Settings → Root Directory: server
  → Railway auto-detects Node.js
  → Build: npm install (automatic)
  → Start: node server.js (automatic)

  STEP 3: Add MongoDB (2 min)
  → Click "New" → "Database" → "Add MongoDB"
  → Railway creates instance
  → Connection string auto-added
  → Or use MongoDB Atlas

  STEP 4: Add Environment Variables (5 min)
  → Click "Variables" tab
  → Add all required variables (see below)
  → Generate new JWT_SECRET
  → Add your Netlify URL to ALLOWED_ORIGINS

  STEP 5: Deploy (3 min)
  → Railway deploys automatically
  → Wait for "Deployment successful"
  → Copy your Railway URL

  STEP 6: Update Frontend (2 min)
  → Edit js/config.js
  → Update BACKEND_URL with Railway URL
  → Commit and push

  STEP 7: Update Google OAuth (3 min)
  → Google Cloud Console
  → Add Netlify URL to authorized origins
  → Save

  Total Time: ~20 minutes

┌─────────────────────────────────────────────────────────────────────┐
│ ENVIRONMENT VARIABLES FOR RAILWAY                                   │
└─────────────────────────────────────────────────────────────────────┘

  Copy these to Railway Variables tab:

  NODE_ENV=production
  PORT=5000
  
  # MongoDB (if using Atlas)
  MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/luganda-movies
  
  # JWT (Generate new secrets!)
  JWT_SECRET=GENERATE_64_CHAR_SECRET
  JWT_EXPIRES_IN=7d
  JWT_REFRESH_EXPIRES_IN=30d
  
  # Session
  SESSION_SECRET=GENERATE_32_CHAR_SECRET
  
  # Google OAuth
  GOOGLE_CLIENT_ID=573762962600-nr77v5emb2spn7aleg9p2l7c0d6be3a9.apps.googleusercontent.com
  
  # CORS (Update with YOUR Netlify URL)
  ALLOWED_ORIGINS=https://your-site.netlify.app
  CLIENT_URL=https://your-site.netlify.app
  
  # Features
  ENABLE_RATE_LIMITING=true
  ENABLE_LOGGING=true
  TRUST_PROXY=true

  Generate secrets:
  node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

┌─────────────────────────────────────────────────────────────────────┐
│ AFTER RAILWAY DEPLOYMENT                                            │
└─────────────────────────────────────────────────────────────────────┘

  1. Railway gives you a URL like:
     https://luganda-movies-api-production.up.railway.app

  2. Update js/config.js (line 15):
     BACKEND_URL: isLocalhost 
         ? 'http://localhost:5000'
         : 'https://YOUR-RAILWAY-URL.up.railway.app',

  3. Commit and push:
     git add js/config.js
     git commit -m "Update backend URL to Railway"
     git push origin main

  4. Netlify auto-deploys with new config

  5. Test your site!

┌─────────────────────────────────────────────────────────────────────┐
│ MONGODB OPTIONS                                                     │
└─────────────────────────────────────────────────────────────────────┘

  OPTION 1: Railway MongoDB Plugin (Easiest)
  ✅ One-click setup
  ✅ Automatic connection
  ✅ Good for testing
  ⚠️  Limited to 500MB

  How to add:
  → In Railway project, click "New"
  → Select "Database" → "Add MongoDB"
  → Railway creates instance
  → Connection string auto-added

  OPTION 2: MongoDB Atlas (Recommended)
  ✅ 512MB free forever
  ✅ Production-ready
  ✅ Better performance
  ✅ Automatic backups

  How to add:
  → Go to cloud.mongodb.com
  → Create free M0 cluster
  → Get connection string
  → Add to Railway as MONGODB_URI

┌─────────────────────────────────────────────────────────────────────┐
│ TESTING RAILWAY DEPLOYMENT                                          │
└─────────────────────────────────────────────────────────────────────┘

  After deployment, test these endpoints:

  # Health check
  curl https://your-railway-url.up.railway.app/api/health

  # Movies
  curl https://your-railway-url.up.railway.app/api/luganda-movies

  # VJs
  curl https://your-railway-url.up.railway.app/api/vjs

  Expected: All return 200 OK with data

┌─────────────────────────────────────────────────────────────────────┐
│ COMPLETE FLOW                                                       │
└─────────────────────────────────────────────────────────────────────┘

  YOUR SETUP:
  Frontend (Netlify) ←→ Backend (Railway) ←→ MongoDB (Atlas/Railway)

  FLOW:
  1. User visits: https://your-site.netlify.app
  2. Frontend loads config.js
  3. Config detects Netlify environment
  4. API calls go to: https://your-railway-url.up.railway.app
  5. Backend processes request
  6. MongoDB returns data
  7. Backend sends response
  8. Frontend displays content

═══════════════════════════════════════════════════════════════════════

                    🎯 WHAT YOU NEED TO DO NOW

═══════════════════════════════════════════════════════════════════════

  1. DEPLOY TO RAILWAY (15 min)
     → Go to railway.app
     → New Project → Deploy from GitHub
     → Select your repo
     → Set root directory: server
     → Add environment variables
     → Add MongoDB
     → Deploy

  2. GET RAILWAY URL (1 min)
     → Copy from Railway dashboard
     → Example: https://luganda-movies-api-production.up.railway.app

  3. UPDATE CONFIG.JS (1 min)
     → Edit js/config.js line 15
     → Replace with your Railway URL
     → Save

  4. COMMIT AND PUSH (1 min)
     → git add js/config.js
     → git commit -m "Update backend URL"
     → git push origin main

  5. WAIT FOR NETLIFY (2 min)
     → Netlify auto-deploys
     → Check deployment status

  6. TEST (5 min)
     → Open your Netlify site
     → Test Google Sign-In
     → Test movie playback
     → Verify everything works

  Total: ~25 minutes

═══════════════════════════════════════════════════════════════════════

                    📋 ENVIRONMENT VARIABLES

═══════════════════════════════════════════════════════════════════════

  REQUIRED (Add to Railway):

  NODE_ENV=production
  PORT=5000
  MONGODB_URI=your-mongodb-connection-string
  JWT_SECRET=generate-new-64-char-secret
  JWT_EXPIRES_IN=7d
  JWT_REFRESH_EXPIRES_IN=30d
  SESSION_SECRET=generate-new-32-char-secret
  GOOGLE_CLIENT_ID=573762962600-nr77v5emb2spn7aleg9p2l7c0d6be3a9.apps.googleusercontent.com
  ALLOWED_ORIGINS=https://your-netlify-site.netlify.app
  CLIENT_URL=https://your-netlify-site.netlify.app
  TRUST_PROXY=true

  Generate secrets:
  node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

═══════════════════════════════════════════════════════════════════════

                    🎉 YOU'RE READY!

═══════════════════════════════════════════════════════════════════════

  Your backend is 100% ready for Railway deployment!

  ✅ All code tested and working
  ✅ Railway config files created
  ✅ Environment variables documented
  ✅ MongoDB connection ready
  ✅ Security configured
  ✅ CORS ready

  Just deploy to Railway and update one line in config.js!

═══════════════════════════════════════════════════════════════════════

Documentation:
- RAILWAY_DEPLOYMENT_GUIDE.md (Complete guide)
- UPDATE_BACKEND_URL.md (This file)
- NETLIFY_DEPLOYMENT_CHECKLIST.md (Full checklist)

═══════════════════════════════════════════════════════════════════════
