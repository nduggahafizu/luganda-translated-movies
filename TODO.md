# UX / Design + AdSense Readiness TODO

- [x] Fix core CSS syntax and invalid declarations in `css/style.css`
- [x] Improve navigation/header visual hierarchy and reduce clutter
- [x] Improve mobile spacing/tap-target UX and section rhythm
- [ ] Add semantic landmarks to `index.html` (`header`, `main#maincontent`, improved `nav` labeling)
- [ ] Reduce inline styles in `index.html` using existing utility/classes where possible
- [ ] Keep AdSense placement policy-safe with clearer section separation
- [x] Fix `movies.html` frontend bug (`API_BASE` -> `API_CONFIG.BASE_URL`)
- [x] Improve crawl trust signals in `robots.txt` (allow key public discovery pages)
- [ ] Add lightweight content safety filter for ad-facing pages (index/movies)
- [ ] Run critical-path verification (curl + static checks)
