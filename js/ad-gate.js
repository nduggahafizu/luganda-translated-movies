/**
 * Ad Gate — currently disabled.
 * AdCash auto-tag (zoneId qo6uumdnra) was hijacking taps with
 * OnClick/Popunder redirects. Re-enable only with a banner-only
 * zone configured on the AdCash dashboard (Popunder/OnClick off).
 */
(function () {
    // Intentionally no-op. Ads come from Google Ad Manager (GPT) only.
})();
